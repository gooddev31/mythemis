// src/theme.ts
import { createTheme } from '@mui/material/styles';

const customTheme = createTheme({
  palette: {
    primary: {
      main: '#00695c', // Темно-зелений колір для прикладу
    },
    // Додайте інші кольори за потреби
  },
});

export default customTheme;
